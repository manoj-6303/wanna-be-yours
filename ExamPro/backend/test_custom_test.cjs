require('dotenv').config(); const mongoose = require('mongoose'); const jwt = require('jsonwebtoken'); const axios = require('axios'); mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/exampro').then(async () => { const User = require('./models/User.js'); const adminUser = await User.default.findOne({ role: 'admin' }); if (!adminUser) return console.log('no admin'); const token = jwt.sign({ id: adminUser._id }, process.env.JWT_SECRET, { expiresIn: '1d' }); console.log('Generated token'); try { const res = await axios.post('http://localhost:5000/api/v1/weekly-tests', { title: 'Test', level: 1, subject: 'Physics', chapter: 'Calorimetry', difficulty: 'Easy', fee: 0, duration: 15, questionCount: 10, passingPercentage: 60, questionIds: [], status: 'Published' }, { headers: { Authorization: \Bearer \\ } }); console.log('Success:', res.data); } catch (error) { console.error('Error:', error.response ? error.response.data : error.message); } process.exit(0); });
