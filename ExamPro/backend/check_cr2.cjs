const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const count = await mongoose.connection.db.collection('questions').countDocuments({ $or: [ {question: { $regex: 'ight\\\\)' }}, {explanation: { $regex: 'ight\\\\)' }}, {'options.text': { $regex: 'ight\\\\)' }} ] }); console.log('Questions with ight):', count); mongoose.disconnect(); });