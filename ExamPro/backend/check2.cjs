const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const topics = await mongoose.connection.db.collection('questions').distinct('topic', { subject: 'Chemistry' }); console.log('Chem:', topics); const mathTopics = await mongoose.connection.db.collection('questions').distinct('topic', { subject: 'Mathematics' }); console.log('Math:', mathTopics.length, mathTopics); mongoose.disconnect(); });