const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const q = await mongoose.connection.db.collection('questions').findOne({ question: { $regex: 'Rolle' } }); for(let i=0; i<q.question.length; i++) { if (q.question.slice(i, i+4) === 'ight') { console.log('Found ight at', i); console.log('Char before:', q.question.charCodeAt(i-1)); } } mongoose.disconnect(); });