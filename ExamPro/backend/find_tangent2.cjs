const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const q = await mongoose.connection.db.collection('questions').findOne({ $or: [ {question: { $regex: 'Tangent to' }}, {explanation: { $regex: 'Tangent to' }} ] }); if (q) { console.log('Found question ID: ', q._id); console.log('Explanation: ', q.explanation); } else { console.log('Not found'); } mongoose.disconnect(); });