const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const delRes = await mongoose.connection.db.collection('questions').deleteMany({ chapter: 'Area Under Curves' }); console.log('Deleted:', delRes); const updRes = await mongoose.connection.db.collection('questions').updateMany({ chapter: { $exists: false } }, { $set: { chapter: 'Area Under Curves', topic: 'Area Under Curves' } }); console.log('Updated:', updRes); mongoose.disconnect(); });