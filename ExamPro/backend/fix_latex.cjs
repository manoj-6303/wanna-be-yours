const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const questions = await mongoose.connection.db.collection('questions').find({}).toArray(); let updatedCount = 0; for (let q of questions) { let modified = false; const fixStr = (str) => { if (typeof str !== 'string') return str; let newStr = str.replace(/\r?ight\)/g, '\\\\right)').replace(/\r?ight\\\]/g, '\\\\right]').replace(/\r?ight\\\}/g, '\\\\right}'); if (newStr !== str) { modified = true; } return newStr; }; q.question = fixStr(q.question); q.explanation = fixStr(q.explanation); if (q.options) { q.options.forEach(opt => { opt.text = fixStr(opt.text); }); } if (modified) { await mongoose.connection.db.collection('questions').updateOne({ _id: q._id }, { $set: { question: q.question, explanation: q.explanation, options: q.options } }); updatedCount++; } } console.log('Updated questions:', updatedCount); mongoose.disconnect(); });