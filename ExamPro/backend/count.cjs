const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const empty = await mongoose.connection.db.collection('questions').countDocuments({ chapter: { $in: [null, '', undefined] } }); console.log('Empty chapters:', empty); const areaCount = await mongoose.connection.db.collection('questions').countDocuments({ chapter: 'Area Under Curves' }); console.log('Area Under Curves:', areaCount); mongoose.disconnect(); });