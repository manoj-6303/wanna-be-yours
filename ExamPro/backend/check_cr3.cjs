const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const count = await mongoose.connection.db.collection('questions').countDocuments({ $or: [ {question: { $regex: '\r' }}, {explanation: { $regex: '\r' }}, {'options.text': { $regex: '\r' }} ] }); console.log('Questions with carriage returns:', count); mongoose.disconnect(); });