const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/exampro').then(async () => { const questions = await mongoose.connection.db.collection('questions').find({}).toArray(); let updatedCount = 0; for (let q of questions) { let modified = false; const fixStr = (str) => { if (typeof str !== 'string') return str; let newStr = str.replace(/\right\)/g, '\\\\right)').replace(/\right\]/g, '\\\\right]').replace(/\right\\\}/g, '\\\\right}'); newStr = newStr.replace(/\not/g, '\\\\not ').replace(/\rangle/g, '\\\\rangle').replace(/\nu\b/g, '\\\\nu ').replace(/\nabla/g, '\\\\nabla').replace(/\theta/g, '\\\\theta').replace(/\tau\b/g, '\\\\tau ').replace(/\beta\b/g, '\\\\beta ').replace(/\to\b/g, '\\\\to '); if (newStr !== str) { modified = true; } return newStr; }; q.question = fixStr(q.question); q.explanation = fixStr(q.explanation); if (q.options) { q.options.forEach(opt => { opt.text = fixStr(opt.text); }); } if (modified) { await mongoose.connection.db.collection('questions').updateOne({ _id: q._id }, { $set: { question: q.question, explanation: q.explanation, options: q.options } }); updatedCount++; } } console.log('Updated KaTeX errors in', updatedCount, 'questions'); mongoose.disconnect(); });